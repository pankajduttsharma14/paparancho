import { DateTodayPipe } from './date-today.pipe';

describe('DateTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
